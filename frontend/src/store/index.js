import { createStore } from 'vuex'

export default createStore({
  state: {
    name: "Teachers and Students",
    isAuth: false,
    isTeacher: false,
  },
  mutations: {},
  actions: {},
  modules: {}
});